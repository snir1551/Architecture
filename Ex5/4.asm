

la $t1,0X10010000 #מגיע לכתובת
addi $t0,$0,1 #מקדם 8 פעמים את הלולאה
While:
	lw $s0,0($t1) #שם בs0 את הערך שך הכתובת
	beq $t0,9,End #אם שווה ל9 צא מהלולאה
	mult $s0,$s0
	mfhi $s1 #השמת הערך העליון בs1
	beq $s1,0,next #אם החלק בhi שונה מאפס תעשה +1 לt5
	addi $t5,$t5,1 
next: #אם היה שווה ל0 הגיע לפה ולא העלה ב1
	addi $t0,$t0,1 #קידום ב1
	addi $t1,$t1,4 #קידום כתובת ב4
	J While #תחזור לתחילת הלולאה
End: #סוף



